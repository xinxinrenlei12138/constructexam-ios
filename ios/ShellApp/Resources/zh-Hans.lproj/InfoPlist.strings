/* InfoPlist.strings（简体中文）
 ============================================================
 主页图标上显示的名字：中文环境下用这里的值。

 ⚠️⚠️ 为什么中文名放在这里，而不是直接写进 Info.plist 的 CFBundleDisplayName：

   AltServer 会用 Info.plist 里的 CFBundleDisplayName 去 Apple 注册
   **App ID 的名字**，而 Apple 的校验**拒绝非 ASCII 字符** ——
   报错是 "The name for this app is invalid"
   （错误类名 ALTAppleAPIErrorInvalidAppIDName）。

   于是天然有个两难：
     · 直接写中文  → Apple 拒绝，压根装不上
     · 直接写英文  → 能装，但主页图标上是英文名
   → 解法：**Info.plist 里保持 ASCII（满足 Apple 的校验），
     中文名放在本地化文件里（满足设备显示）。**
     读 Info.plist 原值的是签名工具；决定显示什么的是 iOS —— 两者取的不是同一个值。

 判据：**同一个字段被两个系统按不同目的读取时，
       要让"机器读的那个"符合机器的规则，"人看的那个"符合人的习惯。**
 ============================================================ */

"CFBundleDisplayName" = "建工考试助手";
